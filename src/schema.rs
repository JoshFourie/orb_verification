table! {
    true_orbs (id) {
        id -> Int4,
        proof -> Varchar,
        verified_values -> Varchar,
        signature -> Varchar,
        public_key -> Varchar,
        common_reference_string -> Varchar,
    }
}
